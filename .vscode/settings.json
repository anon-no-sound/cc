{
    // // Hide generated .js files from Explorer when a .ts file with the same name exists
    // "files.exclude": {
    //     "**/*.js": {
    //         "when": "$(basename).ts"
    //     }
    // },
    // Make .js files read-only to prevent accidental edits
    "files.readonlyInclude": {
        "**/*.js": true
    }
}